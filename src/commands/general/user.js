const { EmbedBuilder } = require('discord.js');module.exports={name:'user',category:'general',description:'عرض معلومات عضو',async execute(ctx){const m=ctx.targetMemberFromArgs()||ctx.member;const e=new EmbedBuilder().setTitle(`معلومات ${m.user.tag}`).setColor(0x00b894).addFields({name:'ID',value:m.id,inline:true},{name:'Joined',value:`<t:${Math.floor(m.joinedTimestamp/1000)}:R>`,inline:true},{name:'Created',value:`<t:${Math.floor(m.user.createdTimestamp/1000)}:R>`,inline:true});return ctx.reply({embeds:[e]});}};
