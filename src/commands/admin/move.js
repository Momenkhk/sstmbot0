const { ChannelType } = require('discord.js');module.exports={name:'move',category:'admin',description:'سحب عضو الى روم اخر',async execute(ctx){const t=ctx.targetMemberFromArgs();const c=ctx.targetChannelFromArgs(1);if(!t?.voice?.channelId||!c||c.type!==ChannelType.GuildVoice)return ctx.reply('حدد عضو متصل + روم صوتي');await t.voice.setChannel(c);return ctx.reply('✅ تم السحب');}};
