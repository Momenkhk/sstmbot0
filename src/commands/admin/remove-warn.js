module.exports={name:'remove-warn',category:'admin',description:'إزاله تحذير اعضاء',async execute(ctx){const t=ctx.targetMemberFromArgs();if(!t)return ctx.reply('حدد عضو');const i=Number(ctx.args[1]||1)-1;const k=`${ctx.guild.id}:${t.id}`;const w=ctx.state.userWarnings[k]||[];if(i<0||i>=w.length)return ctx.reply('رقم غير صحيح');w.splice(i,1);return ctx.reply('✅ تم حذف التحذير');}};
