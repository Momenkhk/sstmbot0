const { ActivityType } = require('discord.js');module.exports={name:'status',category:'owners',description:'تغيير حالة البوت',async execute(ctx){const m=ctx.args[0];const t=ctx.args.slice(1).join(' ');if(!['online','idle','dnd'].includes(m)||!t)return ctx.reply('status <dnd|online|idle> <text>');await ctx.client.user.setPresence({status:m,activities:[{type:ActivityType.Watching,name:t}]});return ctx.reply('✅ تم تحديث الحالة');}};
