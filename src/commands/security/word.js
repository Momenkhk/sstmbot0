module.exports={name:'word',category:'security',description:'اضافة او ازالة كلمات يعاقب كاتبها',async execute(ctx){const a=ctx.args[0];const w=ctx.args.slice(1).join(' ').toLowerCase();if(!w)return ctx.reply('word <add|remove> <text>');ctx.gState.blockedWords=ctx.gState.blockedWords||[];if(a==='add')ctx.gState.blockedWords=[...new Set([...ctx.gState.blockedWords,w])];if(a==='remove')ctx.gState.blockedWords=ctx.gState.blockedWords.filter(x=>x!==w);return ctx.reply('✅ تم تحديث الكلمات');}};
